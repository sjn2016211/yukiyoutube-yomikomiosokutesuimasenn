services:
  - type: web
    name: itistempest
    env: python
    region: Oregon
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn --port $PORT --host 0.0.0.0 main:app"
    envVars:
    - key: PYTHON_VERSION
      value: 9.56.7
